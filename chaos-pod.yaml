apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: matar-pod-negocio
  namespace: default
spec:
  action: pod-kill
  mode: one 
  selector:
    labelSelectors:
      app: negocio-api